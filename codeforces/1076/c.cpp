#include <iostream>
#include <vector>
using ll = long long;
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t; cin >> t;
    for(;t--;) {
        int n, q; cin >> n >> q;
        vector<int> a(n);
        for (int i = 0; i < n; i++) {
            int x; cin >> x;
            a.at(i) == x;
        }
        vector<int> b(n);
        for (int i = 0; i < n; i++) {
            int x; cin >> x;
            b.at(i) = x;
        }
        for (;q--;) {
            int l, r; cin >> l >> r;
            
        }
    }
    return 0;
}