#include <bits/stdc++.h>
using ll = long long;
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    ll n, m, s, t; cin >> n >> m >> s >> t;
    vector<ll> p(n);
    for (int i = 0; i < m; i++) {
        cin >> p.at(i);
    }
    return 0;
}