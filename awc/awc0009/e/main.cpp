#include <bits/stdc++.h>
using ll = long long;
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, q; cin >> n >> q;
    vector<ll> v(n);
    vector<ll> log(n + 1);
    for (int i = 0; i < n + 1; i++) {
        
    }
    vector<ll> st;
    for (int i = 0; i < n; i++) {
        cin >> v.at(i);
    }
    return 0;
}