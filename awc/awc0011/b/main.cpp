#include <bits/stdc++.h>
using ll = long long;
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    ll h, w, k; cin >> h >> w >> k;
    char c1, c2; cin >> c1 >> c2;
    
    return 0;
}