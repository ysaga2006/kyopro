#include <bits/stdc++.h>
using ll = long long;
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string p; cin >> p;
    int l; cin >> l;
    cout << (p.size() >= l ? "Yes" : "No") << endl;
    return 0;
}