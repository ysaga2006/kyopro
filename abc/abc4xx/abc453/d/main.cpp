#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  ll h, w;
  cin >> h >> w;

  vector<string> v(h);
  for (ll i = 0; i < h; ++i) cin >> v[i];

  return 0;
}