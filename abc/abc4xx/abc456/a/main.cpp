#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  
  ll x; cin >> x;

  if (3 <= x && x <= 18) cout << "Yes" << '\n';
  else cout << "No" << '\n';
  return 0;
}
