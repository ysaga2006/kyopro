#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int q; cin >> q;
    queue<int> que;
    while (q--) {
        int op; cin >> op;
        if (op == 1) {
            int l; cin >> l;
            
        } else if (op == 2) {

        } else {
            int k; cin >> k;
        }
    }
    return 0;
}
